[run]
source = apps
omit = */tests/*, */migrations/*

[report]
exclude_lines =
    pragma: no cover

[html]
# 输出到 static/coverage/，由 WhiteNoise 直接服务（gzip 压缩 + 长缓存），
# 避免走 Django static.serve 纯 Python 逐文件读取导致大量小文件并发拉取打满磁盘 IO
directory = static/coverage
