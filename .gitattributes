# 强制 shell 脚本使用 LF 行尾
# 原因：Windows 下 core.autocrlf=true 会在 checkout 时将 LF 转为 CRLF，
#       导致容器内 exec 报 "no such file or directory"（#!/bin/bash\r 无法解析解释器）
# 注意：新增 .sh 文件需保证在 Linux 容器/服务器中可执行
*.sh text eol=lf
