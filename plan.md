# 文档管理系统优化计划

## 一、增加审核流程

### 后端修改
- [ ] 在 `Document` 模型中添加审核状态字段（如 `audit_status`）
- [ ] 添加审核流程视图：提交审核、审核通过、审核驳回
- [ ] 修改文档检索逻辑：未审核文档不参与检索

### 前端修改
- [ ] 在文档列表中显示审核状态标签
- [ ] 添加审核操作按钮（仅管理员可见）
- [ ] 上传后显示"待审核"状态

## 二、重复文件检测与权限提示

### 后端修改
- [ ] 检测重复文件时返回已有文档信息（文档名、编号）
- [ ] 返回当前用户对已有文档的权限状态（有阅读权限/无权限）
- [ ] 添加权限申请接口

### 前端修改
- [ ] 检测到重复文件时显示提示：文档名 + 编号
- [ ] 提供操作选项：继续上传（作为新版本）/ 放弃上传
- [ ] 如果用户已有权限：提示"您已有该文档的阅读权限"
- [ ] 如果用户无权限：显示"申请访问"按钮

## 三、版本管理增强

### 后端修改
- [ ] 添加 `is_active` 字段标记活跃版本
- [ ] 添加版本切换接口
- [ ] 修改文档列表接口：默认只返回活跃版本

### 前端修改
- [ ] 显示文档版本列表
- [ ] 支持版本切换查看
- [ ] 支持设置活跃版本

## 四、检索优化

### 后端修改
- [ ] 修改检索接口：默认只检索活跃版本
- [ ] 添加版本过滤参数：`?version=latest` / `?version=all`

### 前端修改
- [ ] 搜索结果显示版本信息
- [ ] 支持版本筛选

---

## 当前实现：直接按新版本上传

暂时跳过冲突检测，直接将上传文件作为新版本创建。