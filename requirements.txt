# =========================================================================
# 企业私有化多场景智能 RAG-Agent 统一知识库平台
# =========================================================================

# --- Django & DRF ---
Django==5.2
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
pypinyin==0.55.0
loguru==0.7.3

# --- 数据库 & 向量 ---
psycopg[binary]==3.3.4
pgvector==0.5.0
dj-database-url==3.1.2

# --- Celery + Redis ---
celery==5.6.3
redis==8.0.1
django-redis==7.0.0
django-celery-beat==2.9.0 
django-celery-results==2.6.0

# --- 服务器 ---
gunicorn==26.0.0
uvicorn[standard]==0.51.0
whitenoise==6.12.0

# --- LLM & Embedding ---
openai==2.45.0              # DeepSeek 兼容 OpenAI 协议
langchain==1.3.13            # 仅用于 LLM 抽象和 Prompt 模板
langchain-openai==1.3.5
requests==2.34.2
httpx==0.28.1

# --- 检索 ---
rank_bm25==0.2.2
jieba==0.42.1               # 中文分词（BM25 用）
numpy==2.5.1               

# --- 文档解析 ---
pymupdf==1.28.0            # PDF
python-docx==1.2.0          # Word
markdown==3.10.2               # Markdown
Pillow==12.3.0              # 图片处理
python-magic==0.4.27        # MIME 检测
pyyaml==6.0.3               # YAML 配置文件解析
chardet==7.4.3              # 编码检测
PyPDF2==3.0.1               # PDF 解析

# --- OSS 存储（可选）---
#oss2==2.19.0                # 阿里云 OSS SDK

# --- 工具 & 通用 ---
python-dotenv==1.2.2
pydantic==2.13.4
argon2-cffi==25.1.0         # 密码哈希
python-jose[cryptography]==3.5.0
tiktoken==0.13.0            # Token 计数

# --- 测试 ---
pytest==9.1.1
pytest-django==4.12.0
pytest-mock==3.15.1
pytest-asyncio==1.4.0
factory-boy==3.3.3

# --- 开发工具（可选）---
ipython==9.15.0
django-extensions==4.1

# --- 可选：本地 embedding/rerank 兜底（首次运行按需装）---
# sentence-transformers==5.6.0
# transformers==5.13.1
# torch==2.13.0
